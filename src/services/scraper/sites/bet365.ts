import { HorseOddsScraperFunc } from '../../../types/odds.types';

export const scrapeBet365: HorseOddsScraperFunc = async (url) => {
  // Placeholder function for Bet365 scraper
  throw new Error('Bet365 scraper not implemented yet, url used: ' + url);
}